const domPrefix = "list-todos";
export const emptyTodosSelector = `${domPrefix}-empty-todos`;
export const fetchTodosErrorSelector = `${domPrefix}-fetch-todos-errors`;
export const deleteTodoSelector = `${domPrefix}-delete-todo`
